# 01_当前阶段任务

更新时间：2026-07-09

## 1. 当前阶段判断

当前阶段：P6 真实付费与运营阶段已启动，当前跳过支付阶段，已完成 AI 大模型接入和第一轮真实输出安全加固。

当前工作重心：进入 PC-0011 AI 接口线上服务端部署准备。支付、订单、退款、结算和真实扣款继续暂停。

## 2. 当前任务包

### PC-0011：AI 接口线上服务端部署准备

状态：CloudBase 云托管默认公网域名已部署并通过线上验证；正式 `www` 域名尚未切换到该服务端环境。

目标：把已在本地跑通并完成输出安全加固的 DeepSeek AI 接口部署到支持服务端 API 的线上环境。

待完成：

1. 确认当前国内线上部署是否支持 Next.js 服务端 API 或云函数。
2. 如果当前仍是纯静态托管，需要迁移到云函数 / 云托管 / 支持 Next.js 服务端函数的平台。
3. 线上配置有效 `DEEPSEEK_API_KEY` 和 `DEEPSEEK_MODEL=deepseek-v4-pro`。
4. 用线上域名执行 P6 smoke test。
5. 抽查线上真实 AI 输出。

边界：

- 不接真实支付。
- 不接订单、退款、结算或真实扣款。
- 不把 API Key 写入前端。
- 不把本地 `.env.local` 当作线上配置。

已完成：

1. 新增 `scripts/ai_report_api_check.mjs` 和 `npm run test:ai`。
2. 新增 `Dockerfile` 和 `.dockerignore`，用于 CloudBase 云托管 Git 平台部署。
3. 更新 `06_上线发布/腾讯云国内部署指南.md`，区分真实 AI 服务端部署和静态兜底部署。
4. 更新 `06_上线发布/国内部署检查清单.md`。
5. 创建 `08_阶段交接/P6_AI接口线上服务端部署准备记录.md`。
6. 本地执行 `npm run test:ai -- http://127.0.0.1:3009` 通过。
7. 本地执行 `npm run test:p6 -- http://127.0.0.1:3009` 通过。
8. 当前公开域名 `/api/reports` 返回 HTML，不是 API 响应，说明线上仍不是服务端 API 环境。
9. 本机 `docker --version` 检查超时，未完成本地 Docker 镜像构建验证。
10. 2026-07-09 已完成 CloudBase 云托管 Git 平台部署，服务名 `fengshui-ai-site`，默认公网域名 `https://fengshui-ai-site-279596-9-1302687317.sh.run.tcloudbase.com`。
11. 已修复云端构建失败原因：锁文件与 `npm ci` 不同步、路径别名解析失败、生产构建缺少 `tailwindcss`、运行时端口和监听地址配置。
12. 已执行 `npm run test:ai -- https://fengshui-ai-site-279596-9-1302687317.sh.run.tcloudbase.com`，通过。
13. 已执行 `npm run test:p6 -- https://fengshui-ai-site-279596-9-1302687317.sh.run.tcloudbase.com`，通过。
14. 已修复 P6 smoke 脚本，使其能自动确认 CloudBase 默认测试域名的“页面访问提示”。

阻塞项：

- 默认云托管域名已可用；切换正式 `www.laozuzongxuanxue.cn` 到服务端环境仍属于生产域名变更，需要用户在腾讯云 CDN / 域名配置中确认。
- DeepSeek API Key 已在截图中出现过，正式对外发布前建议重新生成并替换线上环境变量。

### PC-0010：AI 真实输出人工审核与安全加固

状态：已完成第一轮审核和加固。

已完成：

1. 抽测八字、易经、祈福、解梦、住宅风水 5 个代表性功能。
2. 发现模型自行写出具体八字四柱的风险。
3. 已在 `app/api/reports/route.ts` 增加服务级边界和总提示词约束。
4. 已限制 AI 输出不得自行声称完成真实排盘、起卦、图像识别、梦境预兆或风水吉凶断定。
5. 已将报告结构归一化为每组最多 5 条，并补充安全兜底条目。
6. 已创建 `08_阶段交接/P6_AI真实输出人工审核与安全加固记录.md`。

已验证：

- `npm run lint` 通过。
- `npm run build` 通过。
- 5 个真实 AI 样本复测均通过，0 个命中高风险词检查项。
- `npm run test:p6 -- http://127.0.0.1:3008` 通过。

### PC-0009：AI 大模型推算接入

状态：已完成第一版接入。

目标：跳过支付阶段，先使用服务端 AI 根据用户填写资料、服务配置和已审核古籍片段生成文化参考报告。

已完成：

1. 新增 `app/api/reports/route.ts`，通过服务端调用 DeepSeek Chat Completions API。
2. 新增 `.env.example`，记录 `DEEPSEEK_API_KEY` 和 `DEEPSEEK_MODEL`。
3. 前端表单优先请求 `/api/reports`，生成 AI 文化参考报告。
4. 未配置 API Key 或 AI 调用失败时，自动回退为本地测试报告。
5. 报告记录新增 `reportSource` 和 `generationNote`。
6. 报告卡片显示报告来源。
7. 更新 P5 / P6 smoke 脚本到“生成 AI 参考报告”文案。
8. 创建 `08_阶段交接/P6_AI大模型推算接入记录.md`。

边界：

- 不接真实支付。
- 不接订单、退款、结算或真实扣款。
- 不把模拟解锁改成真实支付。
- 不把 API Key 放到前端。
- 不让模型编造古籍出处。
- 不让模型承诺现实结果、改命、治病、投资收益或法律结论。

已验证：

- `npm run lint` 通过。
- `npm run build` 通过。
- `npm run test:p6 -- http://127.0.0.1:3002` 通过。
- 2026-07-08 切换 DeepSeek 后执行 `npm run lint`、`npm run build`，均通过。
- 2026-07-08 第一枚 DeepSeek Key 返回 401 认证失败；用户更换新 Key 后，官方接口直连成功，本地 `http://127.0.0.1:3006/api/reports` 成功返回结构化 AI 报告。
- 2026-07-08 已将 P5 / P6 smoke 脚本的报告等待时间从 8 秒调整为 60 秒；`npm run test:p6 -- http://127.0.0.1:3006` 通过。

待完成：

1. 确认线上部署环境支持 Next.js 服务端 API 或云函数。
2. 线上配置有效 `DEEPSEEK_API_KEY` 和 `DEEPSEEK_MODEL=deepseek-v4-pro`。
3. 使用真实 AI 输出做人工审核。
4. 后续再补支付阶段。

### PC-0008：真实能力开发前专项审核

状态：已暂停，支付相关内容后续补充。

目标：在小范围用户复测准入信号已满足的基础上，确认真实支付、报告保存、真实 AI 开发前的外部资质、产品边界和技术验收条件。

已完成：

1. 创建 `08_阶段交接/P6_真实能力开发前专项审核启动记录.md`。
2. 汇总用户复测达标结果：核心流程 5 / 5，风险边界 3 / 3，付费意愿 4 / 3，模拟解锁理解 5 / 5，隐私不可接受疑虑 0。
3. 明确本阶段不是直接开发真实支付、真实 AI 或完整账户系统。
4. 明确第一版价格信号集中在 40-100 元以内，其中 U004 为 50 元以内，U005 为 40 元以内。

待确认：

1. 微信支付商户号是否已经开通。
2. 正式主体是否已经完成个体工商户升级。
3. 可用微信支付产品：H5、JSAPI、Native 扫码或其他。
4. 结算账户是否已经认证。
5. 支付域名是否已经在微信支付商户平台配置。
6. 退款权限是否已经开通。
7. 第一版真实完整报告价格采用 40 元以内、50 元以内，还是其他价格。
8. 报告正文默认保存 180 天是否可接受。
9. 用户删除报告正文后，订单对账字段继续保留是否可接受。
10. 第一版是否暂不保存图片类资料。
11. 真实 AI 第一版是否只开放部分服务。
12. 客服处理方式使用微信、手机号、邮箱，还是站内留言。

边界：

- 不把“模拟解锁”改成真实扣款按钮。
- 不以前端本地状态作为支付成功依据。
- 不在退款权限未开通前对真实用户收费。
- 不保存支付密码、验证码、银行卡号或完整支付账户标识。
- 不让模型编造古籍出处、承诺现实结果或替代医疗 / 投资 / 法律建议。

### PC-0007：P6 小范围用户复测与真实付费准入验证

状态：已完成准入信号验证。

目标：在当前完整可用 mock 产品基础上，收集足够的小范围真实用户反馈，判断是否可以进入真实支付、报告保存或真实 AI 的具体开发。

已确认：

1. 用户要求开始进入下一阶段。
2. 当前允许进入“小范围用户复测与准入验证”。
3. 当前不允许直接进入真实支付、真实 AI 或完整账户系统开发。

执行内容：

1. 使用 `https://www.laozuzongxuanxue.cn/` 作为第一版正式可测试入口。
2. 至少收集 5 个真实用户完成核心流程的反馈。
3. 记录用户是否理解“传统文化参考、不承诺现实结果”。
4. 记录用户是否理解当前为模拟解锁，不会真实扣款。
5. 记录最想使用的功能、愿付价格区间、隐私疑虑和表单阻碍。
6. 若用户继续反馈视觉、文案、表单或流程问题，优先做小范围整改并再次验证。
7. 用户提供背景音乐文件后，再接入带开关的背景音乐，不做自动强制播放。

进入真实付费开发的最低要求：

1. 至少 5 个真实用户完成核心流程。
2. 至少 3 个用户能准确复述“文化参考、不承诺现实结果”。
3. 至少 3 个用户表达愿意为完整报告付费或给出明确价格区间。
4. 0 个用户误解为真实算命保证、医疗建议、法术服务或官方寺庙承诺。
5. 用户对报告保存和隐私边界没有不可接受疑虑。
6. 微信支付商户号、签约产品、结算账户、退款权限等正式资料另行确认。

当前复测发现：

1. U001 使用 Windows 11 电脑和华为鸿蒙系统手机访问过。
2. U001 能理解网站是传统文化参考，祈福心愿不保证灵验 / 改命 / 治病，报告不替代医疗、投资、法律或重大人生决策。
3. U001 首次反馈时对“当前不会真实扣款，只是 mock 支付流程”不是很理解。
4. 已优先强化 mock 支付文案：统一改为“模拟解锁”，说明不会跳转微信支付、不会输入付款密码、不会从任何账户扣钱、不会产生真实订单。
5. 已补充 Playwright 浏览器级检查：本地桌面和手机视口均跑通首页、祈福心愿页、紫微模拟解锁流程和我的记录页。
6. 已将完整 `playwright` 固定为开发依赖，新增项目内 Chromium headless shell 安装与复测脚本，并更新 P5 / P6 smoke 脚本到新版“生成测试报告 / 模拟解锁”文案。
7. 已进一步清理用户界面中的开发态 `mock` 词，统一改为“测试 / 示例 / 模拟”表达。
8. U001 已完成新版模拟解锁复测，能复述不会跳转微信支付、不会输入付款密码、不会从任何账户扣钱、不会产生真实订单；可计入“理解模拟解锁不会真实扣款用户数”。
9. U002-U005 已完成模拟解锁专项复测，均能复述不会跳转微信支付、不会输入付款密码、不会从任何账户扣钱、不会产生真实订单。
10. 当前“理解模拟解锁不会真实扣款用户数”为 5 / 5，已达标。
11. U002、U003、U004、U005 已完成完整复测补齐：完成核心流程，理解传统文化参考和不承诺现实结果，不会当作医疗、投资、法律或重大人生决策依据；U002、U003 愿意为完整报告付费，价格 100 元以内，U004 愿意为完整报告付费，价格 50 元以内，U005 愿意为完整报告付费，价格 40 元以内；均不担心报告保存、出生信息、联系方式或图片隐私。
12. 当前统计：核心流程 5 / 5，风险边界 3 / 3，付费意愿 4 / 3，模拟解锁理解 5 / 5，隐私不可接受疑虑 0。
13. 小范围用户复测准入信号已满足；进入真实支付、真实 AI 或完整账户系统开发前，仍需专项审核微信支付商户号、签约产品、结算账户、退款权限、订单状态机、回调验签、客服处理和隐私保存策略。

当前执行子阶段（2026-07-07）：

1. 进入 P6 完整复测补齐阶段。
2. U002、U003、U004、U005 已完成完整复测补齐，可计入核心流程、风险边界、付费意愿和隐私验证。
3. 小范围用户复测准入信号已满足。
4. 下一步优先做真实支付 / 报告保存 / 真实 AI 开发前专项审核：
   - 微信支付商户号、签约产品、结算账户和退款权限。
   - 订单状态机、回调验签、异常处理和客服处理方式。
   - 报告保存字段、用户删除 / 导出、隐私告知和数据留存策略。
   - 真实 AI API 的内容安全边界、失败降级和成本控制。
5. 专项审核通过后，才可以进入真实付费开发准备。

边界：

- 不接真实支付。
- 不接真实大模型 API。
- 不保存真实敏感资料到后端。
- 不把线下祈福点灯写成当前已经真实履约。
- 不承诺祈福灵验、改命、治病、消灾或官方寺庙背书。

### PC-0005：创建项目资料树

状态：已完成。

目标：让用户清晰知道项目资料、代码、阶段文档和上线资料分别在哪里。

输出：

- `PROJECT_CONTROL/05_项目资料树.md`

验收：

- 不继续开发功能。
- 不重构代码。
- 不新增产品需求。
- 只做项目资料导航和结构说明。

### PC-0001：建立长期上下文管理系统

状态：已完成。

目标：创建 `/PROJECT_CONTROL/` 作为新会话最小启动上下文。

输出：

- `PROJECT_CONTROL/00_当前项目状态.md`
- `PROJECT_CONTROL/01_当前阶段任务.md`
- `PROJECT_CONTROL/02_决策记录.md`
- `PROJECT_CONTROL/03_禁止事项.md`
- `PROJECT_CONTROL/04_阶段交接.md`

验收：

- 只整理上下文文档。
- 不继续开发功能。
- 不重构代码。
- 不新增产品需求。

## 3. 待处理任务包

### PC-0002：首页视觉改版最终确认

状态：代码调整和技术复验已完成，待用户本地视觉确认。

目标：确认当前黑金八卦风是否作为正式 MVP 全站视觉方向。

已完成：

1. 按用户参考图，将首页、服务详情页、我的记录页和共享组件统一为黑金罗盘/八卦 + 古籍纸张卡片风格。
2. 保留 mock 黄历、mock 支付、mock 报告和风险边界说明。
3. 未接入真实 AI、真实支付或完整账户系统。
4. 已执行 `npm run lint` 和 `npm run build`，均通过。
5. 2026-06-30 已再次执行路由 smoke test、`npm run lint` 和 `npm run build`，均通过。

建议步骤：

1. 用户本地查看全站主要页面效果：首页、任一服务详情页、生成报告后状态、我的记录页。
2. 如需微调，只改视觉相关文件。
3. 再次执行 `npm run lint` 和 `npm run build`。
4. 确认后提交到 GitHub。
5. 完成后更新 `PROJECT_CONTROL/00_当前项目状态.md`、本文件和 `PROJECT_CONTROL/04_阶段交接.md`。

边界：

- 不新增真实 AI。
- 不新增真实支付。
- 不改变 MVP 功能范围。
- 不把 mock 黄历写成真实黄历。

### PC-0003：国内部署准备

状态：进行中。

目标：备案通过后，把网站迁移或同步部署到腾讯云，解决中国境内手机访问 Vercel 不稳定的问题。

前置条件：

- `laozuzongxuanxue.cn` ICP 备案管局审核已通过。
- 当前项目无 API Route / SSR 数据依赖，优先静态托管；如后续接真实支付、订单、大模型 API，再迁移云托管 / 云函数后端。

已完成：

1. 新增 `npm run build:static`，用于生成静态输出目录 `out/`。
2. 新增 `06_上线发布/腾讯云国内部署指南.md`。
3. 新增 `06_上线发布/国内部署检查清单.md`。
4. 2026-07-01 重新执行 `npm run lint`、`npm run build`、`npm run build:static`，均通过。
5. 2026-07-01 补充 ESLint / Git 忽略规则，避免根目录静态导出产物影响源码检查和 Git 状态。
6. 2026-07-01 使用本地静态服务器验证 `out/` 中 `/`、`/service/ziwei/`、`/service/bazi/`、`/records/` 均返回 200。
7. 2026-07-01 重新生成 `cloudbase-static-upload.zip`，用于 CloudBase / 静态托管手动上传兜底。

建议步骤：

1. 提交并推送本次部署准备配置修复。
2. 创建腾讯云 EdgeOne Pages / CloudBase 静态托管项目。
3. 从 GitHub 仓库拉取代码。
4. 配置构建命令 `npm ci && npm run build:static`，输出目录 `out`。
5. 部署成功后先用腾讯云临时地址 smoke test。
6. 绑定 `laozuzongxuanxue.cn` 和 `www.laozuzongxuanxue.cn`。
7. 开启 HTTPS：腾讯云 CDN 控制台将 HTTPS 服务标注为付费服务，免费方案下不得直接开启；如继续使用腾讯云 CDN HTTPS，需要用户确认接受该服务计费。
8. 做手机访问测试。
9. 完成公安备案提醒。

边界：

- 只在备案通过后做正式国内域名解析。
- 不承诺中国大陆所有网络 100% 可访问，必须实测。

### PC-0004：P6 前置设计

状态：已复审，进入完整可用产品实施准备。

目标：进入真实付费与运营前，先设计支付、订单、报告保存、隐私和大模型 API 边界。

必须先设计，不得直接开发：

1. 真实支付流程。
2. 订单状态。
3. 报告保存机制。
4. 用户隐私和数据保留策略。
5. 大模型 API 调用边界。
6. 内容安全和风险话术。

已完成：

1. 创建 `05_技术开发/P6支付与订单设计.md`。
2. 创建 `05_技术开发/P6报告保存与隐私边界.md`。
3. 创建 `05_技术开发/P6退款与异常处理.md`。
4. 创建 `05_技术开发/P6大模型API与内容安全边界.md`。
5. 创建 `07_测试记录/P6小范围用户测试反馈总结.md`。
6. 创建 `08_阶段交接/P6_真实付费与运营阶段_审核报告.md`，评分 82/100。
7. 创建 `08_阶段交接/P6_真实付费与运营阶段_审核修复报告.md`。
8. 补充支付方案建议、回调验签、退款通道、字段级留存、删除导出、客服 SLA、AI schema 和用户反馈统计区。
9. 用户确认第一版支付平台为微信支付。
10. 用户确认测试阶段个人主体仅做产品和技术测试，不真实收款；正式阶段升级个体工商户。
11. 用户确认退款权限正式阶段再开通。

待完成：

1. 确认微信支付商户号、签约产品、结算账户和具体产品权限。
2. 根域名 HTTPS 继续修复或明确第一版不作为正式入口；`www` 的 DNS / CDN / HTTP / HTTPS 已修复。
3. 完整可用产品完成后，再收集小范围用户测试反馈。
4. 用户确认后再进入真实支付 / 报告保存 / 真实 AI 开发。

### PC-0006：P6 完整可用产品实施准备

状态：已完成第一轮整改，进入 PC-0007 小范围用户复测。

目标：在不接真实支付、不接真实 AI、不保存真实敏感资料的前提下，把当前 MVP 打磨为可给小范围用户完整体验的版本。

已确认：

1. `https://www.laozuzongxuanxue.cn/` 返回 200，可暂定为第一版正式可测试入口。
2. 根域名 `https://laozuzongxuanxue.cn/` 仍返回 CDN 514，暂不作为第一版正式测试入口。
3. P6 前置设计重新审核评分为 90/100。

已完成：

1. 已复查线上 `www` 入口主要页面和完整模拟流程。
2. 已修正仍然过期的域名状态记录。
3. 已完成小范围测试前 smoke test。
4. 已新增并执行 `scripts/p6_www_smoke_test.mjs`，验证首页、功能路由、八字表单、测试报告、古籍出处、风险提醒、模拟解锁和我的记录。
5. 已执行 `npm run lint`，通过。
6. 已记录 1 轮小范围用户反馈，问题集中在首页功能简介、祈福心愿说明、文字对比度、功能页单调、表单易用性和背景音乐。
7. 已扩写首页功能入口文案，并补充祈福心愿后续可设计“线下寺庙祈福点灯记录”的说明，同时保留不承诺现实结果边界。
8. 已调整功能详情页：新增小八卦图、古籍与文化说明、使用边界说明。
9. 已调整表单区：双列响应式布局，留言 / 咨询文本框加高，预览区文字对比增强。
10. 已补全公共视觉样式，修复卡片、表单、按钮和印章类名未定义导致的显示不足。
11. 已执行 `npm run lint` 和 `npm run build`，均通过；本地验证 `/` 与 `/service/wish` 返回 200。
12. 背景音乐需求已记录，等待用户提供音频素材后再接入播放器与开关控制。
13. 已明确当前真实用户可见流程中的模拟边界：测试黄历、测试报告、模拟解锁、本机浏览器记录、祈福点灯记录均不代表真实 AI、真实扣款或真实线下履约。
14. 用户已要求开始进入下一阶段，PC-0007 小范围用户复测已启动。

边界：

- 不得直接把模拟解锁改成真实支付。
- 不得直接接入大模型 API。
- 不得绕过合规和隐私设计。

## 4. 每次任务完成后的更新要求

完成任一任务包后，必须更新：

1. `PROJECT_CONTROL/00_当前项目状态.md`
2. `PROJECT_CONTROL/01_当前阶段任务.md`
3. `PROJECT_CONTROL/04_阶段交接.md`

如任务改变项目决策或禁止事项，还必须更新：

- `PROJECT_CONTROL/02_决策记录.md`
- `PROJECT_CONTROL/03_禁止事项.md`
