# 01_当前阶段任务

更新时间：2026-07-01

## 1. 当前阶段判断

当前阶段：P6 真实付费与运营阶段已启动，当前处于前置设计。

当前工作重心：用户已确认线上部署完成；先完成真实支付、订单、报告保存、隐私、大模型 API 和内容安全的前置设计，再决定是否进入真实开发。

## 2. 当前任务包

### PC-0005：创建项目资料树

状态：已完成。

目标：让用户清晰知道项目资料、代码、阶段文档和上线资料分别在哪里。

输出：

- `PROJECT_CONTROL/05_项目资料树.md`

验收：

- 不继续开发功能。
- 不重构代码。
- 不新增产品需求。
- 只做项目资料导航和结构说明。

### PC-0001：建立长期上下文管理系统

状态：已完成。

目标：创建 `/PROJECT_CONTROL/` 作为新会话最小启动上下文。

输出：

- `PROJECT_CONTROL/00_当前项目状态.md`
- `PROJECT_CONTROL/01_当前阶段任务.md`
- `PROJECT_CONTROL/02_决策记录.md`
- `PROJECT_CONTROL/03_禁止事项.md`
- `PROJECT_CONTROL/04_阶段交接.md`

验收：

- 只整理上下文文档。
- 不继续开发功能。
- 不重构代码。
- 不新增产品需求。

## 3. 待处理任务包

### PC-0002：首页视觉改版最终确认

状态：代码调整和技术复验已完成，待用户本地视觉确认。

目标：确认当前黑金八卦风是否作为正式 MVP 全站视觉方向。

已完成：

1. 按用户参考图，将首页、服务详情页、我的记录页和共享组件统一为黑金罗盘/八卦 + 古籍纸张卡片风格。
2. 保留 mock 黄历、mock 支付、mock 报告和风险边界说明。
3. 未接入真实 AI、真实支付或完整账户系统。
4. 已执行 `npm run lint` 和 `npm run build`，均通过。
5. 2026-06-30 已再次执行路由 smoke test、`npm run lint` 和 `npm run build`，均通过。

建议步骤：

1. 用户本地查看全站主要页面效果：首页、任一服务详情页、生成报告后状态、我的记录页。
2. 如需微调，只改视觉相关文件。
3. 再次执行 `npm run lint` 和 `npm run build`。
4. 确认后提交到 GitHub。
5. 完成后更新 `PROJECT_CONTROL/00_当前项目状态.md`、本文件和 `PROJECT_CONTROL/04_阶段交接.md`。

边界：

- 不新增真实 AI。
- 不新增真实支付。
- 不改变 MVP 功能范围。
- 不把 mock 黄历写成真实黄历。

### PC-0003：国内部署准备

状态：进行中。

目标：备案通过后，把网站迁移或同步部署到腾讯云，解决中国境内手机访问 Vercel 不稳定的问题。

前置条件：

- `laozuzongxuanxue.cn` ICP 备案管局审核已通过。
- 当前项目无 API Route / SSR 数据依赖，优先静态托管；如后续接真实支付、订单、大模型 API，再迁移云托管 / 云函数后端。

已完成：

1. 新增 `npm run build:static`，用于生成静态输出目录 `out/`。
2. 新增 `06_上线发布/腾讯云国内部署指南.md`。
3. 新增 `06_上线发布/国内部署检查清单.md`。
4. 2026-07-01 重新执行 `npm run lint`、`npm run build`、`npm run build:static`，均通过。
5. 2026-07-01 补充 ESLint / Git 忽略规则，避免根目录静态导出产物影响源码检查和 Git 状态。
6. 2026-07-01 使用本地静态服务器验证 `out/` 中 `/`、`/service/ziwei/`、`/service/bazi/`、`/records/` 均返回 200。
7. 2026-07-01 重新生成 `cloudbase-static-upload.zip`，用于 CloudBase / 静态托管手动上传兜底。

建议步骤：

1. 提交并推送本次部署准备配置修复。
2. 创建腾讯云 EdgeOne Pages / CloudBase 静态托管项目。
3. 从 GitHub 仓库拉取代码。
4. 配置构建命令 `npm ci && npm run build:static`，输出目录 `out`。
5. 部署成功后先用腾讯云临时地址 smoke test。
6. 绑定 `laozuzongxuanxue.cn` 和 `www.laozuzongxuanxue.cn`。
7. 开启 HTTPS。
8. 做手机访问测试。
9. 完成公安备案提醒。

边界：

- 只在备案通过后做正式国内域名解析。
- 不承诺中国大陆所有网络 100% 可访问，必须实测。

### PC-0004：P6 前置设计

状态：进行中。

目标：进入真实付费与运营前，先设计支付、订单、报告保存、隐私和大模型 API 边界。

必须先设计，不得直接开发：

1. 真实支付流程。
2. 订单状态。
3. 报告保存机制。
4. 用户隐私和数据保留策略。
5. 大模型 API 调用边界。
6. 内容安全和风险话术。

已完成：

1. 创建 `05_技术开发/P6支付与订单设计.md`。
2. 创建 `05_技术开发/P6报告保存与隐私边界.md`。
3. 创建 `05_技术开发/P6退款与异常处理.md`。
4. 创建 `05_技术开发/P6大模型API与内容安全边界.md`。
5. 创建 `07_测试记录/P6小范围用户测试反馈总结.md`。

待完成：

1. 复验正式域名 HTTPS 和 `www` 访问。
2. 收集小范围用户测试反馈。
3. 用户确认后再进入真实支付 / 报告保存 / 真实 AI 开发。

边界：

- 不得直接把 mock 支付改成真实支付。
- 不得直接接入大模型 API。
- 不得绕过合规和隐私设计。

## 4. 每次任务完成后的更新要求

完成任一任务包后，必须更新：

1. `PROJECT_CONTROL/00_当前项目状态.md`
2. `PROJECT_CONTROL/01_当前阶段任务.md`
3. `PROJECT_CONTROL/04_阶段交接.md`

如任务改变项目决策或禁止事项，还必须更新：

- `PROJECT_CONTROL/02_决策记录.md`
- `PROJECT_CONTROL/03_禁止事项.md`
