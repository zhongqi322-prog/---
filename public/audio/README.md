# Background Music

Place the site background music file here as:

```text
public/audio/background.mp3
```

Then enable it with:

```text
NEXT_PUBLIC_BACKGROUND_MUSIC_SRC=/audio/background.mp3
```

The player only appears when this environment variable is configured. Browsers require the visitor to click the music button before playback starts.
