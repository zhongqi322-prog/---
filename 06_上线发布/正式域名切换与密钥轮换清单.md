# 正式域名切换与密钥轮换清单

更新时间：2026-07-09

## 1. 当前结论

CloudBase 云托管默认公网域名已通过线上验收：

```text
https://fengshui-ai-site-279596-9-1302687317.sh.run.tcloudbase.com
```

正式域名 `https://www.laozuzongxuanxue.cn` 已切换到 CloudBase 云托管服务端环境，`/api/reports` 已通过 POST 验证。

## 2. 切换前必须完成

- [ ] 在 DeepSeek 控制台重新生成 API Key。
- [ ] 在 CloudBase 云托管服务 `fengshui-ai-site` 的环境变量中替换 `DEEPSEEK_API_KEY`。
- [x] 保留 `DEEPSEEK_MODEL=deepseek-v4-pro`。
- [x] 重新发布 CloudBase 云托管服务。
- [x] 用默认公网域名重新执行：

```powershell
npm run test:ai -- https://fengshui-ai-site-279596-9-1302687317.sh.run.tcloudbase.com
npm run test:p6 -- https://fengshui-ai-site-279596-9-1302687317.sh.run.tcloudbase.com
```

## 3. 正式 `www` 域名切换

目标：让 `https://www.laozuzongxuanxue.cn` 指向 CloudBase 云托管服务端环境，而不是旧静态托管源站。

腾讯云里只选择一种方式：

1. 推荐方式：在 CloudBase 云托管服务里绑定自定义域名 `www.laozuzongxuanxue.cn`。
2. 备选方式：在 CDN / EdgeOne 中把 `www` 的源站从旧静态源站切换到云托管服务对应源站。

切换时必须确认：

- [x] HTTPS 证书已绑定 `www.laozuzongxuanxue.cn`。
- [x] `www` 的访问路径 `/api/reports` 会到达云托管服务。
- [x] 不修改 DeepSeek API Key 到前端代码。
- [x] 不启用真实支付、订单、退款或真实扣款。

## 4. 切换后验收

正式域名切换后，在本地执行：

```powershell
npm run test:ai -- https://www.laozuzongxuanxue.cn
npm run test:p6 -- https://www.laozuzongxuanxue.cn
```

验收通过标准：

- [x] `/api/reports` 返回结构化 AI 报告。
- [x] 首页、功能页、填表、生成报告、模拟解锁、我的记录流程通过。
- [x] 风险提醒和古籍出处可见。
- [x] 模拟解锁仍不会跳转微信支付、不会输入付款密码、不会扣款、不会产生真实订单。

## 5. 仍未纳入本次切换

- 真实支付、订单、退款、结算：继续暂停。
- 背景音乐：播放器框架已接入，但默认不启用；等待有授权音频文件和 `NEXT_PUBLIC_BACKGROUND_MUSIC_SRC` 配置。
- 根域名 `https://laozuzongxuanxue.cn`：仍需单独处理 CDN / 证书 / 频控问题。
- 公安联网备案：正式对外运营前需要关注。
