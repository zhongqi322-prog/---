# P5测试记录

测试日期：2026-06-19

执行 Agent：发布测试 Agent / 前端开发 Agent

## 1. Skill / 插件使用记录

- 已读取并使用 `$karpathy-guidelines`：用于控制 P5 多步骤工程验证范围。
- 已读取并使用 `webapp-testing`：用于 Playwright 本地流程测试。
- 已读取并评估 `deploy-pipeline`：本项目 P5 只涉及 Vercel 前端部署，不涉及 Stripe / Supabase。当前未使用 Composio 全链路部署。
- Browser 插件未提供可调用工具入口，因此按项目规则使用 Playwright + HTTP 检查替代。

## 2. 本地质量检查

- `npm run lint`：通过。
- `npx tsc --noEmit`：通过。
- `npm run build`：通过。

构建备注：

- Next.js 构建成功。
- Windows 环境仍提示 `@next/swc-win32-x64-msvc` 原生包加载异常，但构建已通过 WASM / webpack 路径完成，不阻塞本地构建。

## 3. HTTP 路由检查

全部返回 200：

- `/`
- `/service/bazi`
- `/service/ziwei`
- `/service/marriage`
- `/service/fengshui`
- `/service/yijing`
- `/service/wish`
- `/service/dream`
- `/service/palm`
- `/service/classics`
- `/records`

## 4. Playwright Smoke Test

测试视口：390px x 844px。

已验证：

- 首页可打开。
- 首页今日黄历卡片可见。
- 八字服务页可打开。
- 表单可填写并提交。
- mock 报告可生成。
- 报告包含古籍出处、书名、原文、白话解释、与本问题的关系和风险提醒。
- mock 支付弹窗可打开。
- mock 支付弹窗明确说明不会产生真实扣款。
- 点击 mock 支付成功后，可显示完整报告已 mock 解锁。
- 我的记录页可读取 localStorage 中的新报告记录。

结果：通过。

## 5. 边界检查

检索范围：

- `app`
- `components`
- `lib`
- `data`
- `00_项目总纲`
- `06_上线发布`
- `08_阶段交接`

检查结果：

- 未发现真实 AI 接入。
- 未发现真实支付接入。
- 未发现账号系统接入。
- 未发现英文版、宝宝起名或分享返佣新增。
- 命中的“真实支付”“保证”“治病”“驱邪”“法术”等词均处于规则、禁止项、风险提醒或历史文档语境中，不构成新增边界违规。

## 6. Vercel 发布检查

已确认：

- 全局 `vercel` 命令未安装。
- 仓库内不存在 `.vercel` 项目绑定目录。
- `npx --yes vercel@latest --version` 可运行，版本为 `54.14.2`。
- 已补充 `06_上线发布/Vercel网页端部署指南.md`。
- 已补充 `scripts/p5_public_smoke_test.py`，用于公开地址 smoke test。
- 已执行 `python -m py_compile scripts/p5_public_smoke_test.py`：通过。
- 已执行 `python scripts/p5_public_smoke_test.py --help`：通过。
- 已用本地生产服务执行 `python scripts/p5_public_smoke_test.py http://127.0.0.1:3000`：通过。

阻塞：

- `npx --yes vercel@latest whoami` 显示没有现有凭证，并尝试进入登录流程。
- 登录流程报错：`Error: 钟翔 @ vercel 54.14.2 node-v24.13.1 win32 (x64) is not a legal HTTP header value`。
- 当前环境无法非交互完成 Vercel 登录、项目绑定和公开部署。
- 已尝试禁用遥测、设置 `CI=1`、覆盖 `USER` / `USERNAME` / `npm_config_user_agent`，仍无法绕过该 CLI 登录错误。

结论：

- 本地发布前检查通过。
- Vercel 公开部署未完成。
- 需用户通过 Vercel 网页端连接 GitHub 仓库部署，或在英文用户名环境中完成 CLI 登录，之后继续执行公开地址 smoke test。

## 7. 是否建议公开给用户测试

暂不建议。

原因：

- 本地流程已通过。
- 公开 Vercel 地址尚未生成。
- 公开地址 smoke test 尚未执行。
